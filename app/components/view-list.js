import ListComponent from 'ember-inspector/components/list';
export default ListComponent.extend({
  /**
   * Passed as an attribute
   *
   * @property pinnedObjectId
   * @type {String}
   */
  pinnedObjectId: null
});
